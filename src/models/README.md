# Models

In questa cartella si definiscono i modelli neurali, le architetture (es. moduli `torch.nn.Module`) e layer custom. Mantenete separate le diverse componenti per un codice modulare e riutilizzabile.
