# Configurazioni Esperimenti

Salvate in questa cartella i file di configurazione (in formato YAML o JSON) utilizzati per i diversi esperimenti effettuati. Ad esempio `baseline.yaml`, `model_v1.yaml`. L'obiettivo è separare la logica del codice dai parametri contingenti di addestramento.
